Scaffold(
        appBar: AppBar(
          backgroundColor: Colors.purple,
          // ignore: prefer_const_constructors
          title: Center(child: Text('This is fun')),
        ),
        // body: Row(
        //   mainAxisAlignment: MainAxisAlignment.spaceEvenly,
        //   crossAxisAlignment: CrossAxisAlignment.end,
        //   children: const [
        //     Icon(Icons.home),
        //     Expanded(
        //       child: Icon(Icons.shopping_cart),
        //     ),
        //     Icon(Icons.settings_applications_outlined)
        //   ],
        // ),
        // ignore: avoid_unnecessary_containers
        //Center(
        //   child: Container(
        //     //this is a body section and the container is like a div tag :)
        //     // ignore: prefer_const_constructors
        //     margin: const EdgeInsets.all(10),
        //     padding: const EdgeInsets.all(10),
        //     height: 45,
        //     width: 185,

        //     decoration: BoxDecoration(
        //         color: Colors.purple, borderRadius: BorderRadius.circular(50)),

        //     //this is a body section and the container is like a div tag :)
        //     // ignore: prefer_const_constructors
        //     child: Center(
        //       child: const Text('Hi, This is a baby 👶'),
        //     ),
        //   ),
        // ),
        // ignore: prefer_const_literals_to_create_immutables
        bottomNavigationBar: BottomNavigationBar(currentIndex: 0,
        showSelectedLabels: false,
        showUnselectedLabels: false,
        selectedItemColor: Colors.purple,
         items: const [
          BottomNavigationBarItem(
            icon: Icon(Icons.message),
            label: 'chat',
          ),
          BottomNavigationBarItem(
            icon: Icon(Icons.people),
            label: 'friends',
          ),
          BottomNavigationBarItem(
            icon: Icon(Icons.settings),
            label: 'settings',
          )
        ]),
      ),
    );
  }
}
* WHAT IS A setState in flutter and how does it work?
a setState is a function that needs to be called whenever some drastric changes are done in the code so that the UI gets updated
 type: BottomNavigationBarType.fixed, it helps to fix the animation of bottomNavigationBar
 Center(
            // ignore: prefer_const_constructors
            child: SpinKitCubeGrid(
          color: const Color.fromARGB(255, 102, 37, 181),
          size: 20,
        )),

this is for loading page with spinkit widget of flutter


class Mainpage extends StatefulWidget {
  const Mainpage({super.key});

  @override
  State<Mainpage> createState() => _MainpageState();
}

class _MainpageState extends State<Mainpage> {
  int currentIndex = 0;
  final screens = [
    const firstpage(),
    const secondpage(),
    const thirdpage(),
    const fourthpage()
  ];
  @override
  Widget build(BuildContext context) => MaterialApp(
        initialRoute: '/1',
        routes: {'/1': (context) => const loading(),
        '/2': (context) => const firstpage()
        },
        debugShowCheckedModeBanner: false,
        home: Scaffold(
          body: screens[currentIndex],
          // ignore: prefer_const_literals_to_create_immutables
          bottomNavigationBar: BottomNavigationBar(
              iconSize: 25,
              // type: BottomNavigationBarType.fixed, it helps to fix the animation
              onTap: (index) => setState(() => currentIndex =
                  index), //setstate is a function that updates the UI immediately
              currentIndex: currentIndex,
              items: const [
                BottomNavigationBarItem(
                    icon: Icon(Icons.home),
                    label: "First",
                    backgroundColor: Color.fromARGB(0, 0, 0, 0)),
                BottomNavigationBarItem(
                    icon: Icon(Icons.message),
                    label: "Second",
                    backgroundColor: Color.fromARGB(50, 141, 2, 240)),
                BottomNavigationBarItem(
                    icon: Icon(Icons.person),
                    label: "Third",
                    backgroundColor: Colors.pinkAccent),
                BottomNavigationBarItem(
                    icon: Icon(Icons.settings),
                    label: "Fourth",
                    backgroundColor: Colors.deepPurple)
              ]),
        ),
      );
}
 routes: {
        '/1': (context) => const loading(),
        '/2': (context) => const firstpage(),
        '/3': (context) => const secondpage(),
        '/4': (context) => const thirdpage(),
        '/5': (context) => const fourthpage(),
      },